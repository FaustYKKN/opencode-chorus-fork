// Keep in sync with package.json "version" (enforced by test/unit/package-manifest
// + mcp-client tests). Reported to the Chorus server as the MCP clientInfo version.
export const PACKAGE_VERSION = "0.10.9"
